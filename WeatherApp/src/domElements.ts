export namespace AllDomEl {
  export const searchButton = document.getElementById(
    "search-button"
  ) as HTMLButtonElement;

  export const searchInput = document.getElementById(
    "search-input"
  ) as HTMLInputElement;
  export const mainContainer = document.getElementById(
    "main"
  ) as HTMLDivElement;
}
